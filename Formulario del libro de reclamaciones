<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>Libro de Reclamaciones</title>
  <style>
   body {
  background: #fdf6f0; /* pastel crema */
  margin: 0;
  padding: 24px;
  display: flex;
  justify-content: center;
  font-family: Arial, sans-serif;
}

.card {
  width: 100%;
  max-width: 600px;
  background: #ffffff;
  border-radius: 16px;
  box-shadow: 0 6px 20px rgba(22,27,80,0.08);
  padding: 24px;
}

h1 {
  margin: 0 0 10px;
  font-size: 24px;
  color: #6c5b7b; /* pastel púrpura */
}

p.lead {
  margin: 0 0 18px;
  color: #7d7d7d;
}

form {
  display: flex;
  flex-direction: column;
  gap: 14px;
}

label {
  font-size: 14px;
  color: #444;
  margin-bottom: 4px;
  display: block;
}

input, select, textarea, button {
  font-size: 15px;
  padding: 10px 12px;
  border: 1px solid #c5d8d1; /* verde pastel suave */
  border-radius: 10px;
  background-color: #f8faf9; /* fondo muy claro */
}

input:focus, select:focus, textarea:focus {
  outline: none;
  border-color: #a3c4bc; /* verde agua pastel */
  box-shadow: 0 0 0 3px rgba(163, 196, 188, 0.2);
}

textarea {
  min-height: 100px;
  resize: vertical;
}

button {
  background: #a3c4bc; /* verde agua pastel */
  color: #fff;
  border: none;
  cursor: pointer;
  font-weight: bold;
  transition: background 0.3s ease;
}

button:hover {
  background: #50f3cd;
}

.entry {
  border: 1px dashed #d3d3d3;
  padding: 10px;
  border-radius: 10px;
  margin-top: 8px;
  background: #fdfdfd;
}

/* Responsive: una columna en móviles */
@media (max-width: 640px) {
  form {
    flex-direction: column;
  }
}
  </style>
</head>
<body>
  <div class="card">
    <h1>Libro de Reclamaciones</h1>
    <p class="lead">Complete el siguiente formulario para registrar su reclamo, sugerencia o consulta.</p>

    <form>
      <label for="empresa">Empresa / Local</label>
      <input type="text" id="empresa" name="empresa" placeholder="Nombre de la empresa o local">

      <label for="tipo">Tipo</label>
      <select id="tipo" name="tipo">
        <option value="">-- Seleccione --</option>
        <option value="reclamo">Reclamo</option>
        <option value="queja">Queja</option>
        <option value="sugerencia">Sugerencia</option>
        <option value="consulta">Consulta</option>
      </select>

      <label for="nombre">Nombre completo</label>
      <input type="text" id="nombre" name="nombre" placeholder="Apellido y nombres">

      <label for="dni">DNI / RUC</label>
      <input type="text" id="dni" name="dni" placeholder="Documento de identidad">

      <label for="correo">Correo electrónico</label>
      <input type="email" id="correo" name="correo" placeholder="correo@ejemplo.com">

      <label for="descripcion">Descripción del reclamo / queja / sugerencia</label>
      <textarea id="descripcion" name="descripcion" placeholder="Escriba aquí su reclamo, queja, sugerencia o consulta..."></textarea>

      <button type="submit">Enviar</button>
    </form>
  </div>
</body>
</html>
